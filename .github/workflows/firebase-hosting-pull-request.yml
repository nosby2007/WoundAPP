# The main branch is the unrelated legacy touch app, NOT the Ionic WoundAPP.
# The mobile application and its PR checks live on master.
# Deliberately no pull_request or push trigger here; do not deploy touch into
# JADE DEV Hosting or the production mobile site.
name: Legacy touch preview disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  explain:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy touch previews disabled; use WoundAPP master PR workflow."
